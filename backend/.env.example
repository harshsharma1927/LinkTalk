# Server
PORT=5000
NODE_ENV=development

# MongoDB - replace with your connection string
MONGODB_URI=mongodb://localhost:27017/chatapp

# JWT - generate a strong random secret in production (e.g. openssl rand -hex 32)
JWT_SECRET=y7f3a9c2b8e1d4a6f9c0b2e5d7a8f1c3d
JWT_EXPIRES_IN=7d

# CORS - use * for all origins, or e.g. http://localhost:3000 (no trailing slash)
CORS_ORIGIN=*